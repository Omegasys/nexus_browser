#[test]
fn site_data_can_be_tracked() {
    let site = "example.com";

    assert!(!site.is_empty());
}

#[test]
fn site_data_can_be_cleared() {
    let cleared = true;

    assert!(cleared);
}

#[test]
fn all_site_data_can_be_cleared() {
    let cleared = true;

    assert!(cleared);
}
