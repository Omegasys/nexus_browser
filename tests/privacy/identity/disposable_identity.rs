#[test]
fn disposable_identity_can_be_created() {
    let disposable = true;

    assert!(disposable);
}

#[test]
fn disposable_identity_can_be_destroyed() {
    let destroyed = true;

    assert!(destroyed);
}
